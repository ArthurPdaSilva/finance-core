# Frontend example environment variables
# Copy to frontend/.env and fill values before running the frontend

# API key used to authenticate requests to the backend (match backend API_KEY)
API_KEY=your_api_key_here

# Backend API URL (development)
API_URL=http://127.0.0.1:8000
